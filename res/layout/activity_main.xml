<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity" >

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_alignParentBottom="true"
        android:id="@+id/login"
        android:textSize="20sp"
        android:textColor="#ffffff"
        android:padding="10dp"
        android:background="@drawable/login_button"
        android:text="@string/login"/>
    <ImageView 
        android:id="@+id/barras"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/login"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/app_name"
        android:src="@drawable/barras"/>
    <View 
        android:background="@drawable/card"
        android:layout_width="match_parent"
        android:layout_margin="10dp"
        android:id="@+id/view_card"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_above="@id/barras"/>
    <ImageButton 
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignRight="@id/view_card"
        android:layout_alignTop="@id/view_card"
        android:src="@drawable/logofinal"
        android:id="@+id/tweet_main"
        android:scaleType="centerInside"
        android:background="@android:color/transparent"
        android:contentDescription="@string/app_name"
        android:layout_margin="10dp"
        />
    <TextView 
        android:text="@string/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/view_card"
        android:layout_alignLeft="@id/view_card"
        android:layout_margin="10dp"
        android:textSize="22sp"
        android:textStyle="italic"
        android:textColor="#1dcaff"/>
</RelativeLayout>
